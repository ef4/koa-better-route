{
  "name": "koa-better-route",
  "description": "Like koa-route, except it works with koa-compose",
  "repository": "https://github.com/cardstack/koa-better-route",
  "version": "0.3.0",
  "keywords": [
    "koa",
    "middleware",
    "routes",
    "router",
    "route"
  ],
  "files": [
    "index.js",
    "index.d.ts"
  ],
  "devDependencies": {
    "@types/koa": "^2.0.52",
    "istanbul-harmony": "0",
    "koa": "^2.0.0-alpha.3",
    "mocha": "^2.2.5",
    "mocha-eslint": "^3.0.1",
    "should": "*",
    "supertest": "0"
  },
  "license": "MIT",
  "dependencies": {
    "debug": "*",
    "methods": "~1.1.0",
    "path-to-regexp": "^1.2.0"
  },
  "peerDependencies": {
    "koa": "2",
    "@types/koa": "2"
  },
  "scripts": {
    "test": "NODE_ENV=test mocha --harmony --require should --reporter spec",
    "test-cov": "NODE_ENV=test node --harmony ./node_modules/.bin/istanbul cover ./node_modules/.bin/_mocha -- --require should",
    "test-travis": "NODE_ENV=test node --harmony ./node_modules/.bin/istanbul cover ./node_modules/.bin/_mocha --report lcovonly -- --require should"
  }
}
